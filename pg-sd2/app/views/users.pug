doctype html
html
  head
    title Users
  body
    h1 Users

    if users.length
      ul
        each user in users
          li
            a(href=`/users/${user.id}`)= user.name
    else
      p No users found.